version: '3.8'
services:
  nginx:
    image: nginx:alpine
    ports:
      - "8000:80"
    volumes:
      - ./requirements:/etc/nginx/templates
    networks:

    environment:
      - NGINX_HOST=foobar.com
      - NGINX_PORT=80
  mariadb:
    networks:



  wordpress:
    networks:
##################################################
volumes:


networks:


